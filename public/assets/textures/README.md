# Textures

Place texture files here.

## Planned files

- `court_wood.jpg`      — hardwood floor diffuse map
- `court_wood_spec.jpg` — hardwood specular/roughness map
- `jersey_normal.png`   — jersey fabric normal map (shared)
- `ball_diffuse.png`    — basketball seam/panel texture
