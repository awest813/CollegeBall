# Prop Models

Place court prop GLB files here.

## Planned files

- `hoop_assembly.glb`  — basket stanchion, backboard, rim, net
- `bench.glb`           — player bench
- `scorers_table.glb`   — scorer's table
- `crowd_section.glb`   — arena crowd billboard section (LOD)
